language: generic

os:
  - linux
  - osx

sudo: false

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then export PATH=/opt/ghc/8.4.3/bin:/opt/cabal/head/bin:$PATH; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update; brew install ghc cabal-install gtk+3; fi
  - travis_retry cabal new-update

script:
  - cabal new-build all
  - cabal new-exec -- which slay-gtk-example

addons:
  apt:
    sources:
    - hvr-ghc
    packages:
    - ghc-8.4.3
    - cabal-install-head
    - libgtk-3-dev

cache:
  directories:
    - ~/.cabal
    - ~/.ghc

matrix:
  fast_finish: true
  allow_failures:
  - os: osx
